class Main {
    function void main() {
        var string name;
        var Pikachu p;
        var bool playing;
        let playing = true;
        let p = Pikachu.new();
        while(playing){
            do Screen.drawCircle(p.x, p.y, 5);
        }
        return;
    }
}