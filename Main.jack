class Main {
    function void main() {
        var Pikachu p;
        var boolean playing;
        let playing = true;
        let p = Pikachu.new();
        do Phys.init();
        while(playing) {
            do Screen.drawCircle(200, 200, 5);
        }
        return;
    }
}