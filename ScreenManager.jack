class ScreenManager {
    static Pikachu p;
    static Ball b;
    static Rod r;
    static Floor f;

    function void init(Pikachu pi, Ball ba, Rod ro, Floor fl) {
        let p = pi;
        let b = ba;
        let r = ro;
        let f = fl;
        return;
    }

    function void render() {
        do Screen.clearScreen();
        do Draw.drawFloor();
        do f.render();
        do p.render();
        do b.render();
        do r.render();
        return;
    }

    function void mainlobby(){
        do Screen.clearScreen();
        do Draw.drawTitle();
        return;
    }

    function void playing(){
        // TODO
        return;
    }

    function void end(){
        // TODO
        return;
    }

    // function void drawCircle(int x, int y, int r) {
    //     /** check if there's a problem with the circle. */
    //     // do Output.println();
    //     // do Output.printString("x=");
    //     // do Output.printInt(x);
    //     // do Output.printString(" y=");
    //     // do Output.printInt(y);
    //     // do Output.printString(" r=");
    //     // do Output.printInt(r);
    //     // do Output.println();
    //     // do Sys.wait(2000);
    //     do Screen.drawCircle(x, y, r);
    //     return;
    // }
}