class Phys {
    static Float dt;

    function void init() {
        let dt = Float.new();
        do dt.setInt(1);
        do dt.divInt(1000); // dt = 0.001
        return;
    }

    function Float getDt() {
        return dt;
    }
}